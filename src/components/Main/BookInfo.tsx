import React, { FC, useState, useEffect } from "react";
import axios from "axios";

const BookInfo: FC = () => {
  //   const [worksByAnAuthor, setWorksByAnAuthor] = useState([]);
  //   const [error, setError] = useState(null);

  // const authorKey=

  //   const getWorksByAnAuthor = async () => {
  //     await axios
  //       .get(`https://openlibrary.org${authorKey}/works.json`)
  //       .then(({ data }) => setWorksByAnAuthor(data.entries))
  //       .catch((err) => setError(err));
  //   };

  return <div></div>;
};

export default BookInfo;
